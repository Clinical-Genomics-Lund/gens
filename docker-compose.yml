version: "3.8"
services:
  mongodb:
    container_name: mongodb
    image: mongo
    volumes:
      - ./volumes/mongodb/data:/data/db
    networks:
      - scout-net
  gens:
    build: .
    volumes:
      - ./utils:/home/worker/app/utils
      - ./gens:/home/worker/app/gens
      - ./volumes/gens/data:/home/worker/data
      - ./volumes/gens/wgs:/access/wgs
    environment:
      - FLASK_ENV=development
      - MONGODB_HOST=mongodb
      - MONGODB_PORT=27017
      - SCOUT_DBNAME=scout
      - GENS_DBNAME=gens
    expose:
      - '5000'
    ports:
      - "5003:5000"
    networks:
      - scout-net
    depends_on:
      - mongodb
    command: "flask run --host 0.0.0.0"
  scout:
    image: clinicalgenomics/scout
    container_name: scout-web
    expose:
      - '5000'
    ports:
      - '5000:5000'
    command: scout --host mongodb serve --host 0.0.0.0
    networks:
      - scout-net
    depends_on:
      - mongodb
networks:
  scout-net:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.22.0.0/24
